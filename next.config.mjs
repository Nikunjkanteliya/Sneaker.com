/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['plus.unsplash.com', 'cdn.allbirds.com', 'static.nike.com', 'secure-images.nike.com', 'images.vans.com', 'web-images.credcdn.in', 'www.chromeproductions.com'],
  },

};

export default nextConfig;
